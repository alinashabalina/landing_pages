import { defineComponent } from "vue";

export default defineComponent({
  name: "TariffButton",
  props: {
    btext: {
      type: String,
      required: true,
    }
  },
  components: {
  },
  setup() {},

  return: {},
});